<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Udrw編集: <%= udrw.pid %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>
<body>
  <div class="container mt-5">
    <h1 class="text-center mb-4">Udrw編集: <%= udrw.pid %></h1>

    <form action="/uradrw/edit/<%= udrw._id %>" method="POST" class="col-lg-6 mx-auto">

      <div class="mb-3">
        <label for="pid" class="form-label">PID (商品番号)</label>
        <input type="text" class="form-control" id="pid" name="pid" value="<%= udrw.pid %>" readonly>
      </div>

      <div class="mb-3">
        <label for="name" class="form-label">Name</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= udrw.name %>" required>
      </div>

      <div class="mb-3">
        <label for="relationalBid" class="form-label">関連モデル (bid: name)</label>
        <select class="form-select" id="relationalBid" name="relationalBid" required>
          <% prostitutes.forEach(prst => { %>
            <option value="<%= prst.bid %>" <%= udrw.relationalBid === prst.bid ? 'selected' : '' %>>
              <%= prst.bid %>：<%= prst.name %>
            </option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3">
        <label for="originalStatus" class="form-label">Original Link Status</label>
        <select class="form-select" id="originalStatus" name="originalStatus" required>
          <% ['active', 'hidden', 'deleted'].forEach(status => { %>
            <option value="<%= status %>" <%= udrw.link.original.status === status ? 'selected' : '' %>>
              <%= status %>
            </option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3">
        <label for="archiveUrl" class="form-label">Archive Link URL (オプション)</label>
        <input type="text" class="form-control" id="archiveUrl" name="archiveUrl" value="<%= udrw.link.archive.url !== '-' ? udrw.link.archive.url : '' %>" placeholder="空欄の場合 '-' が設定されます">
      </div>

      <div class="mb-3">
        <label for="archiveStatus" class="form-label">Archive Link Status</label>
        <select class="form-select" id="archiveStatus" name="archiveStatus" required>
          <% ['none', 'active', 'hidden', 'deleted'].forEach(status => { %>
            <option value="<%= status %>" <%= udrw.link.archive.status === status ? 'selected' : '' %>>
              <%= status %>
            </option>
          <% }) %>
        </select>
      </div>

      <div class="mb-3 border p-3 rounded">
        <strong>画像アーカイブステータス (再試行対象):</strong>
        <ul class="list-unstyled">
        <% udrw.images.forEach((image, index) => { %>
          <% if (image.archive.status === 'none') { %>
            <li>
              <span class="badge bg-danger">要再試行</span> Index <%= index %>: <%= image.original.url %>
            </li>
          <% } %>
        <% }) %>
        </ul>
        <small class="form-text text-muted">※保存時に、archive.statusが「none」の画像はアーカイブ処理を再試行します。</small>
      </div>

      <div class="d-grid gap-2">
        <button type="submit" class="btn btn-warning">変更を保存</button>
        <a href="/uradrw" class="btn btn-secondary">一覧へ戻る</a>
      </div>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
</body>
</html>